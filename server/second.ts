export const bro = "20";
import slugify from "slugify";

export const slugged = slugify('SOMe KIND String', {
    lower: true
})
export const dabaav = async () => {
    return new Promise((resolve, reject) => {
        resolve(20);
    });
}